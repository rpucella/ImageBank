{% extends "layout.nj" %}


{% block container %}

  <h1>{{pagetitle}}</h1>

  {{ pager(page, total, base) }}
  
  {% for img in images %}
  {{ imagerow(img) }}  
  {% endfor %}

  {{ pager(page, total, base) }}
  
{% endblock %}
    
